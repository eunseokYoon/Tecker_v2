[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
# --ds 는 컨테이너의 DJANGO_SETTINGS_MODULE 환경변수(dev)보다 우선한다.
addopts = -v --tb=short --ds=config.settings.test
